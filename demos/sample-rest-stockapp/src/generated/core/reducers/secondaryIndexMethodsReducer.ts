/*
 * WARNING: DO NOT EDIT THIS FILE. This file is generated by yarn gen. Any changes will be overwritten.
 */

import * as I from 'immutable';
import {
    SecondaryIndexMethodsAction,
    INDEX_INFORMATION_SUCCESSFUL,
    INDEX_INFORMATION_FAILED,
    INDEX_INFORMATION_IN_PROGRESS,
    CREATE_INDEX_SUCCESSFUL,
    CREATE_INDEX_FAILED,
    CREATE_INDEX_IN_PROGRESS,
    GET_INDEX_STATS_SUCCESSFUL,
    GET_INDEX_STATS_FAILED,
    GET_INDEX_STATS_IN_PROGRESS,
    DROP_INDEX_SUCCESSFUL,
    DROP_INDEX_FAILED,
    DROP_INDEX_IN_PROGRESS,
} from '../actions/secondaryIndexMethodsActions';
import { ApiState } from '../state/ApiState';
import { createTypedMap } from '../../../core/types/TypedMap';

export default function secondaryIndexMethodsReducer(
    state: ApiState,
    action: SecondaryIndexMethodsAction
): ApiState {
    if (state == null) {
        // No state yet. create initial
        state = createTypedMap();
    } else if (!I.Iterable.isIterable(state)) {
        // State plain object. Convert to immutable.
        state = I.fromJS(state);
    }

    switch (action.type) {
        case INDEX_INFORMATION_IN_PROGRESS:
            state = state.setIn(['secondaryIndexMethods', 'indexInformation', 'inProgress'], true);
            break;
        case INDEX_INFORMATION_SUCCESSFUL:
            {
                const payload = I.fromJS(action.payload);
                state = state.withMutations(map =>
                    map
                        .setIn(['secondaryIndexMethods', 'indexInformation', 'inProgress'], false)
                        .removeIn(['secondaryIndexMethods', 'indexInformation', 'errorValue'])
                        .setIn(
                            ['secondaryIndexMethods', 'indexInformation', 'successValue'],
                            payload
                        )
                        .setIn(['last', 'successValue'], payload)
                        .removeIn(['last', 'errorValue'])
                );
            }
            break;
        case INDEX_INFORMATION_FAILED:
            {
                const payload = I.fromJS(action.payload);
                state = state.withMutations(map =>
                    map
                        .setIn(['secondaryIndexMethods', 'indexInformation', 'inProgress'], false)
                        .removeIn(['secondaryIndexMethods', 'indexInformation', 'successValue'])
                        .setIn(
                            ['secondaryIndexMethods', 'indexInformation', 'errorValue'],
                            I.fromJS(action.payload)
                        )
                        .removeIn(['last', 'successValue'])
                        .setIn(['last', 'errorValue'], payload)
                );
            }
            break;

        case CREATE_INDEX_IN_PROGRESS:
            state = state.setIn(['secondaryIndexMethods', 'createIndex', 'inProgress'], true);
            break;
        case CREATE_INDEX_SUCCESSFUL:
            {
                const payload = I.fromJS(action.payload);
                state = state.withMutations(map =>
                    map
                        .setIn(['secondaryIndexMethods', 'createIndex', 'inProgress'], false)
                        .removeIn(['secondaryIndexMethods', 'createIndex', 'errorValue'])
                        .setIn(['secondaryIndexMethods', 'createIndex', 'successValue'], payload)
                        .setIn(['last', 'successValue'], payload)
                        .removeIn(['last', 'errorValue'])
                );
            }
            break;
        case CREATE_INDEX_FAILED:
            {
                const payload = I.fromJS(action.payload);
                state = state.withMutations(map =>
                    map
                        .setIn(['secondaryIndexMethods', 'createIndex', 'inProgress'], false)
                        .removeIn(['secondaryIndexMethods', 'createIndex', 'successValue'])
                        .setIn(
                            ['secondaryIndexMethods', 'createIndex', 'errorValue'],
                            I.fromJS(action.payload)
                        )
                        .removeIn(['last', 'successValue'])
                        .setIn(['last', 'errorValue'], payload)
                );
            }
            break;

        case GET_INDEX_STATS_IN_PROGRESS:
            state = state.setIn(['secondaryIndexMethods', 'getIndexStats', 'inProgress'], true);
            break;
        case GET_INDEX_STATS_SUCCESSFUL:
            {
                const payload = I.fromJS(action.payload);
                state = state.withMutations(map =>
                    map
                        .setIn(['secondaryIndexMethods', 'getIndexStats', 'inProgress'], false)
                        .removeIn(['secondaryIndexMethods', 'getIndexStats', 'errorValue'])
                        .setIn(['secondaryIndexMethods', 'getIndexStats', 'successValue'], payload)
                        .setIn(['last', 'successValue'], payload)
                        .removeIn(['last', 'errorValue'])
                );
            }
            break;
        case GET_INDEX_STATS_FAILED:
            {
                const payload = I.fromJS(action.payload);
                state = state.withMutations(map =>
                    map
                        .setIn(['secondaryIndexMethods', 'getIndexStats', 'inProgress'], false)
                        .removeIn(['secondaryIndexMethods', 'getIndexStats', 'successValue'])
                        .setIn(
                            ['secondaryIndexMethods', 'getIndexStats', 'errorValue'],
                            I.fromJS(action.payload)
                        )
                        .removeIn(['last', 'successValue'])
                        .setIn(['last', 'errorValue'], payload)
                );
            }
            break;

        case DROP_INDEX_IN_PROGRESS:
            state = state.setIn(['secondaryIndexMethods', 'dropIndex', 'inProgress'], true);
            break;
        case DROP_INDEX_SUCCESSFUL:
            {
                const payload = I.fromJS(action.payload);
                state = state.withMutations(map =>
                    map
                        .setIn(['secondaryIndexMethods', 'dropIndex', 'inProgress'], false)
                        .removeIn(['secondaryIndexMethods', 'dropIndex', 'errorValue'])
                        .setIn(['secondaryIndexMethods', 'dropIndex', 'successValue'], payload)
                        .setIn(['last', 'successValue'], payload)
                        .removeIn(['last', 'errorValue'])
                );
            }
            break;
        case DROP_INDEX_FAILED:
            {
                const payload = I.fromJS(action.payload);
                state = state.withMutations(map =>
                    map
                        .setIn(['secondaryIndexMethods', 'dropIndex', 'inProgress'], false)
                        .removeIn(['secondaryIndexMethods', 'dropIndex', 'successValue'])
                        .setIn(
                            ['secondaryIndexMethods', 'dropIndex', 'errorValue'],
                            I.fromJS(action.payload)
                        )
                        .removeIn(['last', 'successValue'])
                        .setIn(['last', 'errorValue'], payload)
                );
            }
            break;
    }

    return state;
}
